<%= form_for(@post226) do |f| %>
  <% if @post226.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post226.errors.count, "error") %> prohibited this post226 from being saved:</h2>

      <ul>
      <% @post226.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
