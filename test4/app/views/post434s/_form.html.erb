<%= form_for(@post434) do |f| %>
  <% if @post434.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post434.errors.count, "error") %> prohibited this post434 from being saved:</h2>

      <ul>
      <% @post434.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
