<%= form_for(@post452) do |f| %>
  <% if @post452.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post452.errors.count, "error") %> prohibited this post452 from being saved:</h2>

      <ul>
      <% @post452.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
