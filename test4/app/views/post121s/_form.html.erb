<%= form_for(@post121) do |f| %>
  <% if @post121.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post121.errors.count, "error") %> prohibited this post121 from being saved:</h2>

      <ul>
      <% @post121.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
