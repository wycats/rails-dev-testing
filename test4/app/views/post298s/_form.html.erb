<%= form_for(@post298) do |f| %>
  <% if @post298.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post298.errors.count, "error") %> prohibited this post298 from being saved:</h2>

      <ul>
      <% @post298.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
