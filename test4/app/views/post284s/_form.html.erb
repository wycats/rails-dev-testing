<%= form_for(@post284) do |f| %>
  <% if @post284.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post284.errors.count, "error") %> prohibited this post284 from being saved:</h2>

      <ul>
      <% @post284.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
