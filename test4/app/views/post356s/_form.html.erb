<%= form_for(@post356) do |f| %>
  <% if @post356.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post356.errors.count, "error") %> prohibited this post356 from being saved:</h2>

      <ul>
      <% @post356.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
