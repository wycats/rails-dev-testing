<%= form_for(@post420) do |f| %>
  <% if @post420.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post420.errors.count, "error") %> prohibited this post420 from being saved:</h2>

      <ul>
      <% @post420.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
