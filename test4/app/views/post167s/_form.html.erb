<%= form_for(@post167) do |f| %>
  <% if @post167.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post167.errors.count, "error") %> prohibited this post167 from being saved:</h2>

      <ul>
      <% @post167.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
