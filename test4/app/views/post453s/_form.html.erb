<%= form_for(@post453) do |f| %>
  <% if @post453.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post453.errors.count, "error") %> prohibited this post453 from being saved:</h2>

      <ul>
      <% @post453.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
