<%= form_for(@post383) do |f| %>
  <% if @post383.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post383.errors.count, "error") %> prohibited this post383 from being saved:</h2>

      <ul>
      <% @post383.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
