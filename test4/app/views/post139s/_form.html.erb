<%= form_for(@post139) do |f| %>
  <% if @post139.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post139.errors.count, "error") %> prohibited this post139 from being saved:</h2>

      <ul>
      <% @post139.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
