<%= form_for(@post402) do |f| %>
  <% if @post402.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post402.errors.count, "error") %> prohibited this post402 from being saved:</h2>

      <ul>
      <% @post402.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
