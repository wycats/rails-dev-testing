<%= form_for(@post23) do |f| %>
  <% if @post23.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post23.errors.count, "error") %> prohibited this post23 from being saved:</h2>

      <ul>
      <% @post23.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
