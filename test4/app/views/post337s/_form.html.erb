<%= form_for(@post337) do |f| %>
  <% if @post337.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post337.errors.count, "error") %> prohibited this post337 from being saved:</h2>

      <ul>
      <% @post337.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
