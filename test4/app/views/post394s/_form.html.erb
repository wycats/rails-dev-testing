<%= form_for(@post394) do |f| %>
  <% if @post394.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post394.errors.count, "error") %> prohibited this post394 from being saved:</h2>

      <ul>
      <% @post394.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
