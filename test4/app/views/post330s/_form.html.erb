<%= form_for(@post330) do |f| %>
  <% if @post330.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post330.errors.count, "error") %> prohibited this post330 from being saved:</h2>

      <ul>
      <% @post330.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
