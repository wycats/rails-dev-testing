<%= form_for(@post254) do |f| %>
  <% if @post254.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post254.errors.count, "error") %> prohibited this post254 from being saved:</h2>

      <ul>
      <% @post254.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
