<%= form_for(@post265) do |f| %>
  <% if @post265.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post265.errors.count, "error") %> prohibited this post265 from being saved:</h2>

      <ul>
      <% @post265.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
