<%= form_for(@post454) do |f| %>
  <% if @post454.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post454.errors.count, "error") %> prohibited this post454 from being saved:</h2>

      <ul>
      <% @post454.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
