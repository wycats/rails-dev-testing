<%= form_for(@post479) do |f| %>
  <% if @post479.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post479.errors.count, "error") %> prohibited this post479 from being saved:</h2>

      <ul>
      <% @post479.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
