<%= form_for(@post494) do |f| %>
  <% if @post494.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post494.errors.count, "error") %> prohibited this post494 from being saved:</h2>

      <ul>
      <% @post494.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
