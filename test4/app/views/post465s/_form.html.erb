<%= form_for(@post465) do |f| %>
  <% if @post465.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post465.errors.count, "error") %> prohibited this post465 from being saved:</h2>

      <ul>
      <% @post465.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
