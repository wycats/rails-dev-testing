<%= form_for(@post499) do |f| %>
  <% if @post499.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post499.errors.count, "error") %> prohibited this post499 from being saved:</h2>

      <ul>
      <% @post499.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
