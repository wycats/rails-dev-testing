<%= form_for(@post297) do |f| %>
  <% if @post297.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post297.errors.count, "error") %> prohibited this post297 from being saved:</h2>

      <ul>
      <% @post297.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
