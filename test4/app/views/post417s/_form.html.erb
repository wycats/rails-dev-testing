<%= form_for(@post417) do |f| %>
  <% if @post417.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post417.errors.count, "error") %> prohibited this post417 from being saved:</h2>

      <ul>
      <% @post417.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
