<%= form_for(@post266) do |f| %>
  <% if @post266.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post266.errors.count, "error") %> prohibited this post266 from being saved:</h2>

      <ul>
      <% @post266.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
