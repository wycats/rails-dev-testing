<%= form_for(@post359) do |f| %>
  <% if @post359.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post359.errors.count, "error") %> prohibited this post359 from being saved:</h2>

      <ul>
      <% @post359.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
