<%= form_for(@post363) do |f| %>
  <% if @post363.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post363.errors.count, "error") %> prohibited this post363 from being saved:</h2>

      <ul>
      <% @post363.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
