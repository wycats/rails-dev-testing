<%= form_for(@post351) do |f| %>
  <% if @post351.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post351.errors.count, "error") %> prohibited this post351 from being saved:</h2>

      <ul>
      <% @post351.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
