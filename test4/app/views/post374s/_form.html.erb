<%= form_for(@post374) do |f| %>
  <% if @post374.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post374.errors.count, "error") %> prohibited this post374 from being saved:</h2>

      <ul>
      <% @post374.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
