<%= form_for(@post324) do |f| %>
  <% if @post324.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post324.errors.count, "error") %> prohibited this post324 from being saved:</h2>

      <ul>
      <% @post324.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
