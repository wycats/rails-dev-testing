<%= form_for(@post426) do |f| %>
  <% if @post426.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post426.errors.count, "error") %> prohibited this post426 from being saved:</h2>

      <ul>
      <% @post426.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
