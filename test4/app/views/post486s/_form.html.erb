<%= form_for(@post486) do |f| %>
  <% if @post486.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post486.errors.count, "error") %> prohibited this post486 from being saved:</h2>

      <ul>
      <% @post486.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
