<%= form_for(@post222) do |f| %>
  <% if @post222.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post222.errors.count, "error") %> prohibited this post222 from being saved:</h2>

      <ul>
      <% @post222.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
