<%= form_for(@post418) do |f| %>
  <% if @post418.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post418.errors.count, "error") %> prohibited this post418 from being saved:</h2>

      <ul>
      <% @post418.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
