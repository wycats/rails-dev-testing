<%= form_for(@post408) do |f| %>
  <% if @post408.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post408.errors.count, "error") %> prohibited this post408 from being saved:</h2>

      <ul>
      <% @post408.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
