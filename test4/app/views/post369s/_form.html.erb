<%= form_for(@post369) do |f| %>
  <% if @post369.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post369.errors.count, "error") %> prohibited this post369 from being saved:</h2>

      <ul>
      <% @post369.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
