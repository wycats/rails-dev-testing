<%= form_for(@post33) do |f| %>
  <% if @post33.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post33.errors.count, "error") %> prohibited this post33 from being saved:</h2>

      <ul>
      <% @post33.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
