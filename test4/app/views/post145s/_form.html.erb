<%= form_for(@post145) do |f| %>
  <% if @post145.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post145.errors.count, "error") %> prohibited this post145 from being saved:</h2>

      <ul>
      <% @post145.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
