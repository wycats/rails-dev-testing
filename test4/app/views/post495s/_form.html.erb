<%= form_for(@post495) do |f| %>
  <% if @post495.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post495.errors.count, "error") %> prohibited this post495 from being saved:</h2>

      <ul>
      <% @post495.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
