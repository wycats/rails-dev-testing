<%= form_for(@post344) do |f| %>
  <% if @post344.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post344.errors.count, "error") %> prohibited this post344 from being saved:</h2>

      <ul>
      <% @post344.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
