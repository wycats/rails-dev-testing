<%= form_for(@post271) do |f| %>
  <% if @post271.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post271.errors.count, "error") %> prohibited this post271 from being saved:</h2>

      <ul>
      <% @post271.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
