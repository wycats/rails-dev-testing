<%= form_for(@post135) do |f| %>
  <% if @post135.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post135.errors.count, "error") %> prohibited this post135 from being saved:</h2>

      <ul>
      <% @post135.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
