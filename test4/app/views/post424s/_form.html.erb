<%= form_for(@post424) do |f| %>
  <% if @post424.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post424.errors.count, "error") %> prohibited this post424 from being saved:</h2>

      <ul>
      <% @post424.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
