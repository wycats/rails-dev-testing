<%= form_for(@post230) do |f| %>
  <% if @post230.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post230.errors.count, "error") %> prohibited this post230 from being saved:</h2>

      <ul>
      <% @post230.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
