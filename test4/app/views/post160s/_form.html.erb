<%= form_for(@post160) do |f| %>
  <% if @post160.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post160.errors.count, "error") %> prohibited this post160 from being saved:</h2>

      <ul>
      <% @post160.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
