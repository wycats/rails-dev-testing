<%= form_for(@post305) do |f| %>
  <% if @post305.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post305.errors.count, "error") %> prohibited this post305 from being saved:</h2>

      <ul>
      <% @post305.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
