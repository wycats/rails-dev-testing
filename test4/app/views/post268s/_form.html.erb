<%= form_for(@post268) do |f| %>
  <% if @post268.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post268.errors.count, "error") %> prohibited this post268 from being saved:</h2>

      <ul>
      <% @post268.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
