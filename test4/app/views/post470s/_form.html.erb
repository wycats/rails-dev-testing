<%= form_for(@post470) do |f| %>
  <% if @post470.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post470.errors.count, "error") %> prohibited this post470 from being saved:</h2>

      <ul>
      <% @post470.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
