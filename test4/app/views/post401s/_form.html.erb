<%= form_for(@post401) do |f| %>
  <% if @post401.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post401.errors.count, "error") %> prohibited this post401 from being saved:</h2>

      <ul>
      <% @post401.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
