<%= form_for(@post425) do |f| %>
  <% if @post425.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post425.errors.count, "error") %> prohibited this post425 from being saved:</h2>

      <ul>
      <% @post425.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
