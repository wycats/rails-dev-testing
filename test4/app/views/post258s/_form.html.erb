<%= form_for(@post258) do |f| %>
  <% if @post258.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post258.errors.count, "error") %> prohibited this post258 from being saved:</h2>

      <ul>
      <% @post258.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
