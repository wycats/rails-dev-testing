<%= form_for(@post72) do |f| %>
  <% if @post72.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post72.errors.count, "error") %> prohibited this post72 from being saved:</h2>

      <ul>
      <% @post72.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
