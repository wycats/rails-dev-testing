<%= form_for(@post224) do |f| %>
  <% if @post224.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post224.errors.count, "error") %> prohibited this post224 from being saved:</h2>

      <ul>
      <% @post224.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
