<%= form_for(@post64) do |f| %>
  <% if @post64.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post64.errors.count, "error") %> prohibited this post64 from being saved:</h2>

      <ul>
      <% @post64.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
