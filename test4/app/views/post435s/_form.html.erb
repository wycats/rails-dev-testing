<%= form_for(@post435) do |f| %>
  <% if @post435.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post435.errors.count, "error") %> prohibited this post435 from being saved:</h2>

      <ul>
      <% @post435.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
