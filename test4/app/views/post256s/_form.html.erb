<%= form_for(@post256) do |f| %>
  <% if @post256.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post256.errors.count, "error") %> prohibited this post256 from being saved:</h2>

      <ul>
      <% @post256.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
