<%= form_for(@post447) do |f| %>
  <% if @post447.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post447.errors.count, "error") %> prohibited this post447 from being saved:</h2>

      <ul>
      <% @post447.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
