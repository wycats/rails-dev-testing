<%= form_for(@post46) do |f| %>
  <% if @post46.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post46.errors.count, "error") %> prohibited this post46 from being saved:</h2>

      <ul>
      <% @post46.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
