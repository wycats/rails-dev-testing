<%= form_for(@post335) do |f| %>
  <% if @post335.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post335.errors.count, "error") %> prohibited this post335 from being saved:</h2>

      <ul>
      <% @post335.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
