<%= form_for(@post410) do |f| %>
  <% if @post410.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post410.errors.count, "error") %> prohibited this post410 from being saved:</h2>

      <ul>
      <% @post410.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
