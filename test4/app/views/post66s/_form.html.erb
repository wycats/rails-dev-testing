<%= form_for(@post66) do |f| %>
  <% if @post66.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post66.errors.count, "error") %> prohibited this post66 from being saved:</h2>

      <ul>
      <% @post66.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
