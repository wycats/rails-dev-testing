<%= form_for(@post291) do |f| %>
  <% if @post291.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post291.errors.count, "error") %> prohibited this post291 from being saved:</h2>

      <ul>
      <% @post291.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
