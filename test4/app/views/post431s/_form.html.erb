<%= form_for(@post431) do |f| %>
  <% if @post431.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post431.errors.count, "error") %> prohibited this post431 from being saved:</h2>

      <ul>
      <% @post431.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
