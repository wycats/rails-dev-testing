<%= form_for(@post498) do |f| %>
  <% if @post498.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post498.errors.count, "error") %> prohibited this post498 from being saved:</h2>

      <ul>
      <% @post498.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
