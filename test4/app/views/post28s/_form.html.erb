<%= form_for(@post28) do |f| %>
  <% if @post28.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post28.errors.count, "error") %> prohibited this post28 from being saved:</h2>

      <ul>
      <% @post28.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
