<%= form_for(@post411) do |f| %>
  <% if @post411.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post411.errors.count, "error") %> prohibited this post411 from being saved:</h2>

      <ul>
      <% @post411.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
