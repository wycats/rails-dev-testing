<%= form_for(@post491) do |f| %>
  <% if @post491.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post491.errors.count, "error") %> prohibited this post491 from being saved:</h2>

      <ul>
      <% @post491.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
