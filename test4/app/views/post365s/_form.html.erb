<%= form_for(@post365) do |f| %>
  <% if @post365.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post365.errors.count, "error") %> prohibited this post365 from being saved:</h2>

      <ul>
      <% @post365.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
