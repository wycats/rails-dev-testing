<%= form_for(@post496) do |f| %>
  <% if @post496.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post496.errors.count, "error") %> prohibited this post496 from being saved:</h2>

      <ul>
      <% @post496.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
