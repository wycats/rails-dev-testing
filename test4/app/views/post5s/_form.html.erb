<%= form_for(@post5) do |f| %>
  <% if @post5.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post5.errors.count, "error") %> prohibited this post5 from being saved:</h2>

      <ul>
      <% @post5.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
