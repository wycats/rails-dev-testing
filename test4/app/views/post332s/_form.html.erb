<%= form_for(@post332) do |f| %>
  <% if @post332.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post332.errors.count, "error") %> prohibited this post332 from being saved:</h2>

      <ul>
      <% @post332.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
