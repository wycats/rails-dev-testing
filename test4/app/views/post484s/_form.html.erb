<%= form_for(@post484) do |f| %>
  <% if @post484.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post484.errors.count, "error") %> prohibited this post484 from being saved:</h2>

      <ul>
      <% @post484.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
