<%= form_for(@post287) do |f| %>
  <% if @post287.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post287.errors.count, "error") %> prohibited this post287 from being saved:</h2>

      <ul>
      <% @post287.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
