<%= form_for(@post101) do |f| %>
  <% if @post101.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post101.errors.count, "error") %> prohibited this post101 from being saved:</h2>

      <ul>
      <% @post101.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
