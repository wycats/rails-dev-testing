<%= form_for(@post386) do |f| %>
  <% if @post386.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post386.errors.count, "error") %> prohibited this post386 from being saved:</h2>

      <ul>
      <% @post386.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
