<%= form_for(@post317) do |f| %>
  <% if @post317.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post317.errors.count, "error") %> prohibited this post317 from being saved:</h2>

      <ul>
      <% @post317.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
