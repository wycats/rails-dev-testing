<%= form_for(@post384) do |f| %>
  <% if @post384.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post384.errors.count, "error") %> prohibited this post384 from being saved:</h2>

      <ul>
      <% @post384.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
