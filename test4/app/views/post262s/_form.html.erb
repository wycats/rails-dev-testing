<%= form_for(@post262) do |f| %>
  <% if @post262.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post262.errors.count, "error") %> prohibited this post262 from being saved:</h2>

      <ul>
      <% @post262.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
