<%= form_for(@post373) do |f| %>
  <% if @post373.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post373.errors.count, "error") %> prohibited this post373 from being saved:</h2>

      <ul>
      <% @post373.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
