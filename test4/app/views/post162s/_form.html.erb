<%= form_for(@post162) do |f| %>
  <% if @post162.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post162.errors.count, "error") %> prohibited this post162 from being saved:</h2>

      <ul>
      <% @post162.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
