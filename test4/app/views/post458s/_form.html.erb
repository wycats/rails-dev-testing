<%= form_for(@post458) do |f| %>
  <% if @post458.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post458.errors.count, "error") %> prohibited this post458 from being saved:</h2>

      <ul>
      <% @post458.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
