<%= form_for(@post432) do |f| %>
  <% if @post432.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post432.errors.count, "error") %> prohibited this post432 from being saved:</h2>

      <ul>
      <% @post432.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
