<%= form_for(@post331) do |f| %>
  <% if @post331.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post331.errors.count, "error") %> prohibited this post331 from being saved:</h2>

      <ul>
      <% @post331.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
