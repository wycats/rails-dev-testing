<%= form_for(@post433) do |f| %>
  <% if @post433.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post433.errors.count, "error") %> prohibited this post433 from being saved:</h2>

      <ul>
      <% @post433.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
