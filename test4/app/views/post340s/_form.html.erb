<%= form_for(@post340) do |f| %>
  <% if @post340.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post340.errors.count, "error") %> prohibited this post340 from being saved:</h2>

      <ul>
      <% @post340.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
