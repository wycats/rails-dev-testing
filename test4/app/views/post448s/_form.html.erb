<%= form_for(@post448) do |f| %>
  <% if @post448.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post448.errors.count, "error") %> prohibited this post448 from being saved:</h2>

      <ul>
      <% @post448.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
