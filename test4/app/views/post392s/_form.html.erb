<%= form_for(@post392) do |f| %>
  <% if @post392.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post392.errors.count, "error") %> prohibited this post392 from being saved:</h2>

      <ul>
      <% @post392.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
