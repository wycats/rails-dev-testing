<%= form_for(@post380) do |f| %>
  <% if @post380.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post380.errors.count, "error") %> prohibited this post380 from being saved:</h2>

      <ul>
      <% @post380.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
