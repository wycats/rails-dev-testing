<%= form_for(@post260) do |f| %>
  <% if @post260.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post260.errors.count, "error") %> prohibited this post260 from being saved:</h2>

      <ul>
      <% @post260.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
