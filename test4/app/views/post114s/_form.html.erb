<%= form_for(@post114) do |f| %>
  <% if @post114.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post114.errors.count, "error") %> prohibited this post114 from being saved:</h2>

      <ul>
      <% @post114.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
