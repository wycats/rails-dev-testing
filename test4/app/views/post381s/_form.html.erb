<%= form_for(@post381) do |f| %>
  <% if @post381.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post381.errors.count, "error") %> prohibited this post381 from being saved:</h2>

      <ul>
      <% @post381.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
