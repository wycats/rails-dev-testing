<%= form_for(@post478) do |f| %>
  <% if @post478.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post478.errors.count, "error") %> prohibited this post478 from being saved:</h2>

      <ul>
      <% @post478.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
