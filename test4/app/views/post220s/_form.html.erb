<%= form_for(@post220) do |f| %>
  <% if @post220.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post220.errors.count, "error") %> prohibited this post220 from being saved:</h2>

      <ul>
      <% @post220.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
