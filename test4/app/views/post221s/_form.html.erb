<%= form_for(@post221) do |f| %>
  <% if @post221.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post221.errors.count, "error") %> prohibited this post221 from being saved:</h2>

      <ul>
      <% @post221.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
