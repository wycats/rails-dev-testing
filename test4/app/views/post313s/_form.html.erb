<%= form_for(@post313) do |f| %>
  <% if @post313.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post313.errors.count, "error") %> prohibited this post313 from being saved:</h2>

      <ul>
      <% @post313.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
