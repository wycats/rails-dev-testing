<%= form_for(@post342) do |f| %>
  <% if @post342.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post342.errors.count, "error") %> prohibited this post342 from being saved:</h2>

      <ul>
      <% @post342.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
