<%= form_for(@post158) do |f| %>
  <% if @post158.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post158.errors.count, "error") %> prohibited this post158 from being saved:</h2>

      <ul>
      <% @post158.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
