<%= form_for(@post388) do |f| %>
  <% if @post388.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post388.errors.count, "error") %> prohibited this post388 from being saved:</h2>

      <ul>
      <% @post388.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
