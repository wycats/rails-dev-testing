<%= form_for(@post346) do |f| %>
  <% if @post346.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post346.errors.count, "error") %> prohibited this post346 from being saved:</h2>

      <ul>
      <% @post346.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
