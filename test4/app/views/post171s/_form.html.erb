<%= form_for(@post171) do |f| %>
  <% if @post171.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post171.errors.count, "error") %> prohibited this post171 from being saved:</h2>

      <ul>
      <% @post171.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
