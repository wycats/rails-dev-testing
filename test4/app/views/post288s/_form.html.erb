<%= form_for(@post288) do |f| %>
  <% if @post288.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post288.errors.count, "error") %> prohibited this post288 from being saved:</h2>

      <ul>
      <% @post288.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
