<%= form_for(@post170) do |f| %>
  <% if @post170.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post170.errors.count, "error") %> prohibited this post170 from being saved:</h2>

      <ul>
      <% @post170.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
