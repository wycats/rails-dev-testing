<%= form_for(@post243) do |f| %>
  <% if @post243.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post243.errors.count, "error") %> prohibited this post243 from being saved:</h2>

      <ul>
      <% @post243.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
