<%= form_for(@post471) do |f| %>
  <% if @post471.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post471.errors.count, "error") %> prohibited this post471 from being saved:</h2>

      <ul>
      <% @post471.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
