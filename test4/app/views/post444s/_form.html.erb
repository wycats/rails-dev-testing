<%= form_for(@post444) do |f| %>
  <% if @post444.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post444.errors.count, "error") %> prohibited this post444 from being saved:</h2>

      <ul>
      <% @post444.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
