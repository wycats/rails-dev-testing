<%= form_for(@post492) do |f| %>
  <% if @post492.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post492.errors.count, "error") %> prohibited this post492 from being saved:</h2>

      <ul>
      <% @post492.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
