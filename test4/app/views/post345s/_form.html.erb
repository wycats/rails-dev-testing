<%= form_for(@post345) do |f| %>
  <% if @post345.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post345.errors.count, "error") %> prohibited this post345 from being saved:</h2>

      <ul>
      <% @post345.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
