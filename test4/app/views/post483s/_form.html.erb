<%= form_for(@post483) do |f| %>
  <% if @post483.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post483.errors.count, "error") %> prohibited this post483 from being saved:</h2>

      <ul>
      <% @post483.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
