<%= form_for(@post249) do |f| %>
  <% if @post249.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post249.errors.count, "error") %> prohibited this post249 from being saved:</h2>

      <ul>
      <% @post249.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
