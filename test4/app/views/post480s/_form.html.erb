<%= form_for(@post480) do |f| %>
  <% if @post480.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post480.errors.count, "error") %> prohibited this post480 from being saved:</h2>

      <ul>
      <% @post480.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
