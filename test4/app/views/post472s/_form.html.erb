<%= form_for(@post472) do |f| %>
  <% if @post472.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post472.errors.count, "error") %> prohibited this post472 from being saved:</h2>

      <ul>
      <% @post472.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
