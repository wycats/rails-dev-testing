<%= form_for(@post316) do |f| %>
  <% if @post316.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post316.errors.count, "error") %> prohibited this post316 from being saved:</h2>

      <ul>
      <% @post316.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
