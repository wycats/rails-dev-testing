<%= form_for(@post333) do |f| %>
  <% if @post333.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post333.errors.count, "error") %> prohibited this post333 from being saved:</h2>

      <ul>
      <% @post333.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
