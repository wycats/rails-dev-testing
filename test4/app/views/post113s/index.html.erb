<h1>Listing post113s</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @post113s.each do |post113| %>
  <tr>
    <td><%= link_to 'Show', post113 %></td>
    <td><%= link_to 'Edit', edit_post113_path(post113) %></td>
    <td><%= link_to 'Destroy', post113, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post113', new_post113_path %>
