<%= form_for(@post113) do |f| %>
  <% if @post113.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post113.errors.count, "error") %> prohibited this post113 from being saved:</h2>

      <ul>
      <% @post113.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
