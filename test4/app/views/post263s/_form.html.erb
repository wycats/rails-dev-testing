<%= form_for(@post263) do |f| %>
  <% if @post263.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post263.errors.count, "error") %> prohibited this post263 from being saved:</h2>

      <ul>
      <% @post263.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
