<%= form_for(@post464) do |f| %>
  <% if @post464.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post464.errors.count, "error") %> prohibited this post464 from being saved:</h2>

      <ul>
      <% @post464.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
