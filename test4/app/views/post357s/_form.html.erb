<%= form_for(@post357) do |f| %>
  <% if @post357.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post357.errors.count, "error") %> prohibited this post357 from being saved:</h2>

      <ul>
      <% @post357.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
