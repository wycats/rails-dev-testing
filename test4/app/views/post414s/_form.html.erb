<%= form_for(@post414) do |f| %>
  <% if @post414.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post414.errors.count, "error") %> prohibited this post414 from being saved:</h2>

      <ul>
      <% @post414.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
