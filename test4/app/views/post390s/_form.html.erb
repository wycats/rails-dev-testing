<%= form_for(@post390) do |f| %>
  <% if @post390.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post390.errors.count, "error") %> prohibited this post390 from being saved:</h2>

      <ul>
      <% @post390.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
