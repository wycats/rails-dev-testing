<%= form_for(@post270) do |f| %>
  <% if @post270.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post270.errors.count, "error") %> prohibited this post270 from being saved:</h2>

      <ul>
      <% @post270.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
