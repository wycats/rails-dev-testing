<%= form_for(@post225) do |f| %>
  <% if @post225.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post225.errors.count, "error") %> prohibited this post225 from being saved:</h2>

      <ul>
      <% @post225.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
