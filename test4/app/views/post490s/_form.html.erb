<%= form_for(@post490) do |f| %>
  <% if @post490.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post490.errors.count, "error") %> prohibited this post490 from being saved:</h2>

      <ul>
      <% @post490.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
