<%= form_for(@post413) do |f| %>
  <% if @post413.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post413.errors.count, "error") %> prohibited this post413 from being saved:</h2>

      <ul>
      <% @post413.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
