<%= form_for(@post129) do |f| %>
  <% if @post129.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post129.errors.count, "error") %> prohibited this post129 from being saved:</h2>

      <ul>
      <% @post129.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
