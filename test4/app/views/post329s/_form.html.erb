<%= form_for(@post329) do |f| %>
  <% if @post329.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post329.errors.count, "error") %> prohibited this post329 from being saved:</h2>

      <ul>
      <% @post329.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
