<%= form_for(@post164) do |f| %>
  <% if @post164.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post164.errors.count, "error") %> prohibited this post164 from being saved:</h2>

      <ul>
      <% @post164.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
