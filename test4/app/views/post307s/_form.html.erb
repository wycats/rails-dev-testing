<%= form_for(@post307) do |f| %>
  <% if @post307.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post307.errors.count, "error") %> prohibited this post307 from being saved:</h2>

      <ul>
      <% @post307.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
