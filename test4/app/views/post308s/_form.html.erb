<%= form_for(@post308) do |f| %>
  <% if @post308.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post308.errors.count, "error") %> prohibited this post308 from being saved:</h2>

      <ul>
      <% @post308.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
