<%= form_for(@post16) do |f| %>
  <% if @post16.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post16.errors.count, "error") %> prohibited this post16 from being saved:</h2>

      <ul>
      <% @post16.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
