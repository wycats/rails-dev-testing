<%= form_for(@post29) do |f| %>
  <% if @post29.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post29.errors.count, "error") %> prohibited this post29 from being saved:</h2>

      <ul>
      <% @post29.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
