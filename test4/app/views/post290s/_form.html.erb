<%= form_for(@post290) do |f| %>
  <% if @post290.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post290.errors.count, "error") %> prohibited this post290 from being saved:</h2>

      <ul>
      <% @post290.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
