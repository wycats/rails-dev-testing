<%= form_for(@post299) do |f| %>
  <% if @post299.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post299.errors.count, "error") %> prohibited this post299 from being saved:</h2>

      <ul>
      <% @post299.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
