<%= form_for(@post396) do |f| %>
  <% if @post396.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post396.errors.count, "error") %> prohibited this post396 from being saved:</h2>

      <ul>
      <% @post396.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
