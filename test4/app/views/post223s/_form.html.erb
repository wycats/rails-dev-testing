<%= form_for(@post223) do |f| %>
  <% if @post223.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post223.errors.count, "error") %> prohibited this post223 from being saved:</h2>

      <ul>
      <% @post223.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
