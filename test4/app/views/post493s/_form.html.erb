<%= form_for(@post493) do |f| %>
  <% if @post493.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post493.errors.count, "error") %> prohibited this post493 from being saved:</h2>

      <ul>
      <% @post493.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
