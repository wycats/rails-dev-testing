<%= form_for(@post30) do |f| %>
  <% if @post30.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post30.errors.count, "error") %> prohibited this post30 from being saved:</h2>

      <ul>
      <% @post30.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
