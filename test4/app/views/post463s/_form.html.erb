<%= form_for(@post463) do |f| %>
  <% if @post463.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post463.errors.count, "error") %> prohibited this post463 from being saved:</h2>

      <ul>
      <% @post463.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
