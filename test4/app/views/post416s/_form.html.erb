<%= form_for(@post416) do |f| %>
  <% if @post416.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post416.errors.count, "error") %> prohibited this post416 from being saved:</h2>

      <ul>
      <% @post416.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
