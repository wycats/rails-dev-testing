<h1>Listing post416s</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @post416s.each do |post416| %>
  <tr>
    <td><%= link_to 'Show', post416 %></td>
    <td><%= link_to 'Edit', edit_post416_path(post416) %></td>
    <td><%= link_to 'Destroy', post416, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post416', new_post416_path %>
