<%= form_for(@post429) do |f| %>
  <% if @post429.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post429.errors.count, "error") %> prohibited this post429 from being saved:</h2>

      <ul>
      <% @post429.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
