<%= form_for(@post242) do |f| %>
  <% if @post242.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post242.errors.count, "error") %> prohibited this post242 from being saved:</h2>

      <ul>
      <% @post242.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
