<%= form_for(@post301) do |f| %>
  <% if @post301.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post301.errors.count, "error") %> prohibited this post301 from being saved:</h2>

      <ul>
      <% @post301.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
