<%= form_for(@post273) do |f| %>
  <% if @post273.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post273.errors.count, "error") %> prohibited this post273 from being saved:</h2>

      <ul>
      <% @post273.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
