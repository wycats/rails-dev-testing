<%= form_for(@post159) do |f| %>
  <% if @post159.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post159.errors.count, "error") %> prohibited this post159 from being saved:</h2>

      <ul>
      <% @post159.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
