<%= form_for(@post187) do |f| %>
  <% if @post187.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post187.errors.count, "error") %> prohibited this post187 from being saved:</h2>

      <ul>
      <% @post187.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
