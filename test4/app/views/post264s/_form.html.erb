<%= form_for(@post264) do |f| %>
  <% if @post264.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post264.errors.count, "error") %> prohibited this post264 from being saved:</h2>

      <ul>
      <% @post264.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
