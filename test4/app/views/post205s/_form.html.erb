<%= form_for(@post205) do |f| %>
  <% if @post205.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post205.errors.count, "error") %> prohibited this post205 from being saved:</h2>

      <ul>
      <% @post205.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
