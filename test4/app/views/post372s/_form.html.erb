<%= form_for(@post372) do |f| %>
  <% if @post372.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post372.errors.count, "error") %> prohibited this post372 from being saved:</h2>

      <ul>
      <% @post372.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
