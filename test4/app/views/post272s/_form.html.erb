<%= form_for(@post272) do |f| %>
  <% if @post272.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post272.errors.count, "error") %> prohibited this post272 from being saved:</h2>

      <ul>
      <% @post272.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
