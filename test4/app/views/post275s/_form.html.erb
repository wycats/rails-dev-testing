<%= form_for(@post275) do |f| %>
  <% if @post275.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post275.errors.count, "error") %> prohibited this post275 from being saved:</h2>

      <ul>
      <% @post275.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
