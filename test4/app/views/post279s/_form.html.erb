<%= form_for(@post279) do |f| %>
  <% if @post279.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post279.errors.count, "error") %> prohibited this post279 from being saved:</h2>

      <ul>
      <% @post279.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
