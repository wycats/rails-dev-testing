<%= form_for(@post443) do |f| %>
  <% if @post443.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post443.errors.count, "error") %> prohibited this post443 from being saved:</h2>

      <ul>
      <% @post443.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
