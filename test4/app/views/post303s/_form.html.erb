<%= form_for(@post303) do |f| %>
  <% if @post303.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post303.errors.count, "error") %> prohibited this post303 from being saved:</h2>

      <ul>
      <% @post303.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
