<%= form_for(@post238) do |f| %>
  <% if @post238.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post238.errors.count, "error") %> prohibited this post238 from being saved:</h2>

      <ul>
      <% @post238.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
