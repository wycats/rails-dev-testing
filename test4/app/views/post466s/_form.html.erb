<%= form_for(@post466) do |f| %>
  <% if @post466.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post466.errors.count, "error") %> prohibited this post466 from being saved:</h2>

      <ul>
      <% @post466.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
