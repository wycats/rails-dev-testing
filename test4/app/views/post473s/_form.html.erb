<%= form_for(@post473) do |f| %>
  <% if @post473.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post473.errors.count, "error") %> prohibited this post473 from being saved:</h2>

      <ul>
      <% @post473.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
