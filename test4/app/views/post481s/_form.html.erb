<%= form_for(@post481) do |f| %>
  <% if @post481.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post481.errors.count, "error") %> prohibited this post481 from being saved:</h2>

      <ul>
      <% @post481.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
