<%= form_for(@post349) do |f| %>
  <% if @post349.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post349.errors.count, "error") %> prohibited this post349 from being saved:</h2>

      <ul>
      <% @post349.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
