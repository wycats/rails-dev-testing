<%= form_for(@post57) do |f| %>
  <% if @post57.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post57.errors.count, "error") %> prohibited this post57 from being saved:</h2>

      <ul>
      <% @post57.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
