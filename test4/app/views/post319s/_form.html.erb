<%= form_for(@post319) do |f| %>
  <% if @post319.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post319.errors.count, "error") %> prohibited this post319 from being saved:</h2>

      <ul>
      <% @post319.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
