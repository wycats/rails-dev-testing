<%= form_for(@post40) do |f| %>
  <% if @post40.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post40.errors.count, "error") %> prohibited this post40 from being saved:</h2>

      <ul>
      <% @post40.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
