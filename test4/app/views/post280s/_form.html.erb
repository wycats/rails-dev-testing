<%= form_for(@post280) do |f| %>
  <% if @post280.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post280.errors.count, "error") %> prohibited this post280 from being saved:</h2>

      <ul>
      <% @post280.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
