<%= form_for(@post250) do |f| %>
  <% if @post250.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post250.errors.count, "error") %> prohibited this post250 from being saved:</h2>

      <ul>
      <% @post250.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
