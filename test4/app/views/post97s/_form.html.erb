<%= form_for(@post97) do |f| %>
  <% if @post97.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post97.errors.count, "error") %> prohibited this post97 from being saved:</h2>

      <ul>
      <% @post97.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
