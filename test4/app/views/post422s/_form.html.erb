<%= form_for(@post422) do |f| %>
  <% if @post422.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post422.errors.count, "error") %> prohibited this post422 from being saved:</h2>

      <ul>
      <% @post422.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
