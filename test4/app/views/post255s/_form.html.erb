<%= form_for(@post255) do |f| %>
  <% if @post255.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post255.errors.count, "error") %> prohibited this post255 from being saved:</h2>

      <ul>
      <% @post255.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
