<%= form_for(@post460) do |f| %>
  <% if @post460.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post460.errors.count, "error") %> prohibited this post460 from being saved:</h2>

      <ul>
      <% @post460.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
