<%= form_for(@post364) do |f| %>
  <% if @post364.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post364.errors.count, "error") %> prohibited this post364 from being saved:</h2>

      <ul>
      <% @post364.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
