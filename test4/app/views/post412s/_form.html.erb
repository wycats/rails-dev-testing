<%= form_for(@post412) do |f| %>
  <% if @post412.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post412.errors.count, "error") %> prohibited this post412 from being saved:</h2>

      <ul>
      <% @post412.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
