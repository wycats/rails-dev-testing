<%= form_for(@post259) do |f| %>
  <% if @post259.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post259.errors.count, "error") %> prohibited this post259 from being saved:</h2>

      <ul>
      <% @post259.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
