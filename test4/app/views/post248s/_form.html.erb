<%= form_for(@post248) do |f| %>
  <% if @post248.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post248.errors.count, "error") %> prohibited this post248 from being saved:</h2>

      <ul>
      <% @post248.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
