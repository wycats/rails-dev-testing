<%= form_for(@post153) do |f| %>
  <% if @post153.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post153.errors.count, "error") %> prohibited this post153 from being saved:</h2>

      <ul>
      <% @post153.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
