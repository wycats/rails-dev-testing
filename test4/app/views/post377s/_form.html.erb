<%= form_for(@post377) do |f| %>
  <% if @post377.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post377.errors.count, "error") %> prohibited this post377 from being saved:</h2>

      <ul>
      <% @post377.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
