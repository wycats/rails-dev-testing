<%= form_for(@post103) do |f| %>
  <% if @post103.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post103.errors.count, "error") %> prohibited this post103 from being saved:</h2>

      <ul>
      <% @post103.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
