<%= form_for(@post184) do |f| %>
  <% if @post184.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post184.errors.count, "error") %> prohibited this post184 from being saved:</h2>

      <ul>
      <% @post184.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
