<%= form_for(@post389) do |f| %>
  <% if @post389.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post389.errors.count, "error") %> prohibited this post389 from being saved:</h2>

      <ul>
      <% @post389.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
