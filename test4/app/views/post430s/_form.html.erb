<%= form_for(@post430) do |f| %>
  <% if @post430.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post430.errors.count, "error") %> prohibited this post430 from being saved:</h2>

      <ul>
      <% @post430.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
