<%= form_for(@post485) do |f| %>
  <% if @post485.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post485.errors.count, "error") %> prohibited this post485 from being saved:</h2>

      <ul>
      <% @post485.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
