<%= form_for(@post310) do |f| %>
  <% if @post310.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post310.errors.count, "error") %> prohibited this post310 from being saved:</h2>

      <ul>
      <% @post310.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
