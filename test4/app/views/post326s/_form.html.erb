<%= form_for(@post326) do |f| %>
  <% if @post326.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post326.errors.count, "error") %> prohibited this post326 from being saved:</h2>

      <ul>
      <% @post326.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
