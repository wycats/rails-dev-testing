<%= form_for(@post352) do |f| %>
  <% if @post352.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post352.errors.count, "error") %> prohibited this post352 from being saved:</h2>

      <ul>
      <% @post352.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
