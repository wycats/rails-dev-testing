<%= form_for(@post477) do |f| %>
  <% if @post477.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post477.errors.count, "error") %> prohibited this post477 from being saved:</h2>

      <ul>
      <% @post477.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
