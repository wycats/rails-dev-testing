<%= form_for(@post25) do |f| %>
  <% if @post25.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post25.errors.count, "error") %> prohibited this post25 from being saved:</h2>

      <ul>
      <% @post25.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
