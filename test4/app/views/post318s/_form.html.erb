<%= form_for(@post318) do |f| %>
  <% if @post318.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post318.errors.count, "error") %> prohibited this post318 from being saved:</h2>

      <ul>
      <% @post318.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
