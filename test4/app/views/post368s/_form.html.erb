<%= form_for(@post368) do |f| %>
  <% if @post368.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post368.errors.count, "error") %> prohibited this post368 from being saved:</h2>

      <ul>
      <% @post368.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
