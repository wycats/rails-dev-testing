<%= form_for(@post341) do |f| %>
  <% if @post341.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post341.errors.count, "error") %> prohibited this post341 from being saved:</h2>

      <ul>
      <% @post341.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
