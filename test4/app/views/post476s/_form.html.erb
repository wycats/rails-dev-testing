<%= form_for(@post476) do |f| %>
  <% if @post476.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post476.errors.count, "error") %> prohibited this post476 from being saved:</h2>

      <ul>
      <% @post476.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
