<%= form_for(@post179) do |f| %>
  <% if @post179.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post179.errors.count, "error") %> prohibited this post179 from being saved:</h2>

      <ul>
      <% @post179.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
