<%= form_for(@post232) do |f| %>
  <% if @post232.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post232.errors.count, "error") %> prohibited this post232 from being saved:</h2>

      <ul>
      <% @post232.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
