<%= form_for(@post404) do |f| %>
  <% if @post404.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post404.errors.count, "error") %> prohibited this post404 from being saved:</h2>

      <ul>
      <% @post404.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
