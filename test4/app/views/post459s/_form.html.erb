<%= form_for(@post459) do |f| %>
  <% if @post459.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post459.errors.count, "error") %> prohibited this post459 from being saved:</h2>

      <ul>
      <% @post459.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
