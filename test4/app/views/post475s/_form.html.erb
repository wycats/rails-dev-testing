<%= form_for(@post475) do |f| %>
  <% if @post475.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post475.errors.count, "error") %> prohibited this post475 from being saved:</h2>

      <ul>
      <% @post475.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
