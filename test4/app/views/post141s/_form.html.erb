<%= form_for(@post141) do |f| %>
  <% if @post141.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post141.errors.count, "error") %> prohibited this post141 from being saved:</h2>

      <ul>
      <% @post141.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
