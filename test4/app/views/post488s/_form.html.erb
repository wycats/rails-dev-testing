<%= form_for(@post488) do |f| %>
  <% if @post488.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post488.errors.count, "error") %> prohibited this post488 from being saved:</h2>

      <ul>
      <% @post488.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
