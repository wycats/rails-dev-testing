<%= form_for(@post251) do |f| %>
  <% if @post251.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post251.errors.count, "error") %> prohibited this post251 from being saved:</h2>

      <ul>
      <% @post251.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
