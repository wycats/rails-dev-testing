<%= form_for(@post375) do |f| %>
  <% if @post375.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post375.errors.count, "error") %> prohibited this post375 from being saved:</h2>

      <ul>
      <% @post375.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
