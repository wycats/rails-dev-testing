<%= form_for(@post405) do |f| %>
  <% if @post405.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post405.errors.count, "error") %> prohibited this post405 from being saved:</h2>

      <ul>
      <% @post405.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
