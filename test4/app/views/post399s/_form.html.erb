<%= form_for(@post399) do |f| %>
  <% if @post399.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post399.errors.count, "error") %> prohibited this post399 from being saved:</h2>

      <ul>
      <% @post399.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
