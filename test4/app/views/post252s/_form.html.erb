<%= form_for(@post252) do |f| %>
  <% if @post252.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post252.errors.count, "error") %> prohibited this post252 from being saved:</h2>

      <ul>
      <% @post252.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
