<%= form_for(@post67) do |f| %>
  <% if @post67.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post67.errors.count, "error") %> prohibited this post67 from being saved:</h2>

      <ul>
      <% @post67.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
