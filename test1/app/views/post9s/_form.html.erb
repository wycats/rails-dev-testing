<%= form_for(@post9) do |f| %>
  <% if @post9.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post9.errors.count, "error") %> prohibited this post9 from being saved:</h2>

      <ul>
      <% @post9.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
