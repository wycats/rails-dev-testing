<%= form_for(@post8) do |f| %>
  <% if @post8.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post8.errors.count, "error") %> prohibited this post8 from being saved:</h2>

      <ul>
      <% @post8.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
