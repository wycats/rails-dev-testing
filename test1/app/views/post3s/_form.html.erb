<%= form_for(@post3) do |f| %>
  <% if @post3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post3.errors.count, "error") %> prohibited this post3 from being saved:</h2>

      <ul>
      <% @post3.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
