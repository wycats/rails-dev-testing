<%= form_for(@post37) do |f| %>
  <% if @post37.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post37.errors.count, "error") %> prohibited this post37 from being saved:</h2>

      <ul>
      <% @post37.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
