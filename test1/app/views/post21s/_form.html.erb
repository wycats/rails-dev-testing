<%= form_for(@post21) do |f| %>
  <% if @post21.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post21.errors.count, "error") %> prohibited this post21 from being saved:</h2>

      <ul>
      <% @post21.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
