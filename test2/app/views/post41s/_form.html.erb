<%= form_for(@post41) do |f| %>
  <% if @post41.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post41.errors.count, "error") %> prohibited this post41 from being saved:</h2>

      <ul>
      <% @post41.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
