<%= form_for(@post11) do |f| %>
  <% if @post11.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post11.errors.count, "error") %> prohibited this post11 from being saved:</h2>

      <ul>
      <% @post11.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
