<%= form_for(@post69) do |f| %>
  <% if @post69.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post69.errors.count, "error") %> prohibited this post69 from being saved:</h2>

      <ul>
      <% @post69.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
