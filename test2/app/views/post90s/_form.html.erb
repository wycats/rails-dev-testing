<%= form_for(@post90) do |f| %>
  <% if @post90.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post90.errors.count, "error") %> prohibited this post90 from being saved:</h2>

      <ul>
      <% @post90.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
