<%= form_for(@post4) do |f| %>
  <% if @post4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post4.errors.count, "error") %> prohibited this post4 from being saved:</h2>

      <ul>
      <% @post4.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
