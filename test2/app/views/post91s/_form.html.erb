<%= form_for(@post91) do |f| %>
  <% if @post91.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post91.errors.count, "error") %> prohibited this post91 from being saved:</h2>

      <ul>
      <% @post91.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
