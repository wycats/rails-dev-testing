<%= form_for(@post27) do |f| %>
  <% if @post27.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post27.errors.count, "error") %> prohibited this post27 from being saved:</h2>

      <ul>
      <% @post27.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
