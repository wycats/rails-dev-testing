<%= form_for(@post18) do |f| %>
  <% if @post18.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post18.errors.count, "error") %> prohibited this post18 from being saved:</h2>

      <ul>
      <% @post18.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
