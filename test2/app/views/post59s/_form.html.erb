<%= form_for(@post59) do |f| %>
  <% if @post59.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post59.errors.count, "error") %> prohibited this post59 from being saved:</h2>

      <ul>
      <% @post59.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
