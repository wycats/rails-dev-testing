<%= form_for(@post52) do |f| %>
  <% if @post52.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post52.errors.count, "error") %> prohibited this post52 from being saved:</h2>

      <ul>
      <% @post52.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
