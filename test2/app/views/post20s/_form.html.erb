<%= form_for(@post20) do |f| %>
  <% if @post20.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post20.errors.count, "error") %> prohibited this post20 from being saved:</h2>

      <ul>
      <% @post20.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
