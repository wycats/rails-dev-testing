<%= form_for(@post43) do |f| %>
  <% if @post43.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post43.errors.count, "error") %> prohibited this post43 from being saved:</h2>

      <ul>
      <% @post43.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
