<%= form_for(@post85) do |f| %>
  <% if @post85.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post85.errors.count, "error") %> prohibited this post85 from being saved:</h2>

      <ul>
      <% @post85.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
