<%= form_for(@post34) do |f| %>
  <% if @post34.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post34.errors.count, "error") %> prohibited this post34 from being saved:</h2>

      <ul>
      <% @post34.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
