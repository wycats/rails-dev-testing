<%= form_for(@post79) do |f| %>
  <% if @post79.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post79.errors.count, "error") %> prohibited this post79 from being saved:</h2>

      <ul>
      <% @post79.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
