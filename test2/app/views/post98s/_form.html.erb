<%= form_for(@post98) do |f| %>
  <% if @post98.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post98.errors.count, "error") %> prohibited this post98 from being saved:</h2>

      <ul>
      <% @post98.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
