<%= form_for(@post15) do |f| %>
  <% if @post15.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post15.errors.count, "error") %> prohibited this post15 from being saved:</h2>

      <ul>
      <% @post15.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
