<%= form_for(@post48) do |f| %>
  <% if @post48.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post48.errors.count, "error") %> prohibited this post48 from being saved:</h2>

      <ul>
      <% @post48.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
