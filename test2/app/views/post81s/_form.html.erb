<%= form_for(@post81) do |f| %>
  <% if @post81.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post81.errors.count, "error") %> prohibited this post81 from being saved:</h2>

      <ul>
      <% @post81.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
