<%= form_for(@post22) do |f| %>
  <% if @post22.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post22.errors.count, "error") %> prohibited this post22 from being saved:</h2>

      <ul>
      <% @post22.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
