<%= form_for(@post61) do |f| %>
  <% if @post61.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post61.errors.count, "error") %> prohibited this post61 from being saved:</h2>

      <ul>
      <% @post61.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
