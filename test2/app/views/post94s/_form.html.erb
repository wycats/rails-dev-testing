<%= form_for(@post94) do |f| %>
  <% if @post94.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post94.errors.count, "error") %> prohibited this post94 from being saved:</h2>

      <ul>
      <% @post94.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
