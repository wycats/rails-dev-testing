<%= form_for(@post24) do |f| %>
  <% if @post24.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post24.errors.count, "error") %> prohibited this post24 from being saved:</h2>

      <ul>
      <% @post24.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
