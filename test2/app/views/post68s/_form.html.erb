<%= form_for(@post68) do |f| %>
  <% if @post68.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post68.errors.count, "error") %> prohibited this post68 from being saved:</h2>

      <ul>
      <% @post68.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
