<%= form_for(@post44) do |f| %>
  <% if @post44.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post44.errors.count, "error") %> prohibited this post44 from being saved:</h2>

      <ul>
      <% @post44.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
