<%= form_for(@post84) do |f| %>
  <% if @post84.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post84.errors.count, "error") %> prohibited this post84 from being saved:</h2>

      <ul>
      <% @post84.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
