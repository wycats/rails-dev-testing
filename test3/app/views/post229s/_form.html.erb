<%= form_for(@post229) do |f| %>
  <% if @post229.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post229.errors.count, "error") %> prohibited this post229 from being saved:</h2>

      <ul>
      <% @post229.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
