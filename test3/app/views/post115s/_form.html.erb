<%= form_for(@post115) do |f| %>
  <% if @post115.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post115.errors.count, "error") %> prohibited this post115 from being saved:</h2>

      <ul>
      <% @post115.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
