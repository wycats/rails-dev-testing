<%= form_for(@post132) do |f| %>
  <% if @post132.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post132.errors.count, "error") %> prohibited this post132 from being saved:</h2>

      <ul>
      <% @post132.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
