<%= form_for(@post120) do |f| %>
  <% if @post120.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post120.errors.count, "error") %> prohibited this post120 from being saved:</h2>

      <ul>
      <% @post120.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
