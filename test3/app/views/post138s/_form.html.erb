<%= form_for(@post138) do |f| %>
  <% if @post138.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post138.errors.count, "error") %> prohibited this post138 from being saved:</h2>

      <ul>
      <% @post138.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
