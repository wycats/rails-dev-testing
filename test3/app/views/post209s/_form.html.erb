<%= form_for(@post209) do |f| %>
  <% if @post209.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post209.errors.count, "error") %> prohibited this post209 from being saved:</h2>

      <ul>
      <% @post209.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
