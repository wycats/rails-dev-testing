<%= form_for(@post88) do |f| %>
  <% if @post88.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post88.errors.count, "error") %> prohibited this post88 from being saved:</h2>

      <ul>
      <% @post88.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
