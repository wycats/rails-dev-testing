<%= form_for(@post80) do |f| %>
  <% if @post80.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post80.errors.count, "error") %> prohibited this post80 from being saved:</h2>

      <ul>
      <% @post80.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
