<%= form_for(@post174) do |f| %>
  <% if @post174.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post174.errors.count, "error") %> prohibited this post174 from being saved:</h2>

      <ul>
      <% @post174.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
