<%= form_for(@post134) do |f| %>
  <% if @post134.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post134.errors.count, "error") %> prohibited this post134 from being saved:</h2>

      <ul>
      <% @post134.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
