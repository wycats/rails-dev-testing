<%= form_for(@post219) do |f| %>
  <% if @post219.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post219.errors.count, "error") %> prohibited this post219 from being saved:</h2>

      <ul>
      <% @post219.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
