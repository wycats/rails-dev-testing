<%= form_for(@post216) do |f| %>
  <% if @post216.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post216.errors.count, "error") %> prohibited this post216 from being saved:</h2>

      <ul>
      <% @post216.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
