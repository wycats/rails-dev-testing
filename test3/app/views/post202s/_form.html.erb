<%= form_for(@post202) do |f| %>
  <% if @post202.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post202.errors.count, "error") %> prohibited this post202 from being saved:</h2>

      <ul>
      <% @post202.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
