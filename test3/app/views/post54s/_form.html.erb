<%= form_for(@post54) do |f| %>
  <% if @post54.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post54.errors.count, "error") %> prohibited this post54 from being saved:</h2>

      <ul>
      <% @post54.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
