<%= form_for(@post149) do |f| %>
  <% if @post149.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post149.errors.count, "error") %> prohibited this post149 from being saved:</h2>

      <ul>
      <% @post149.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
