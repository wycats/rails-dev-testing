<%= form_for(@post200) do |f| %>
  <% if @post200.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post200.errors.count, "error") %> prohibited this post200 from being saved:</h2>

      <ul>
      <% @post200.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
