<%= form_for(@post53) do |f| %>
  <% if @post53.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post53.errors.count, "error") %> prohibited this post53 from being saved:</h2>

      <ul>
      <% @post53.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
