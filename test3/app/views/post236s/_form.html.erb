<%= form_for(@post236) do |f| %>
  <% if @post236.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post236.errors.count, "error") %> prohibited this post236 from being saved:</h2>

      <ul>
      <% @post236.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
