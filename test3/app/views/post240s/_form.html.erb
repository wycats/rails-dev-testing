<%= form_for(@post240) do |f| %>
  <% if @post240.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post240.errors.count, "error") %> prohibited this post240 from being saved:</h2>

      <ul>
      <% @post240.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
