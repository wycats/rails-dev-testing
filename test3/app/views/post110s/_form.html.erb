<%= form_for(@post110) do |f| %>
  <% if @post110.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post110.errors.count, "error") %> prohibited this post110 from being saved:</h2>

      <ul>
      <% @post110.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
