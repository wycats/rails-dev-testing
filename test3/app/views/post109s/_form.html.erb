<%= form_for(@post109) do |f| %>
  <% if @post109.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post109.errors.count, "error") %> prohibited this post109 from being saved:</h2>

      <ul>
      <% @post109.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
