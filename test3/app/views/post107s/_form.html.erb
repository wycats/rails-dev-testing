<%= form_for(@post107) do |f| %>
  <% if @post107.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post107.errors.count, "error") %> prohibited this post107 from being saved:</h2>

      <ul>
      <% @post107.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
