<%= form_for(@post213) do |f| %>
  <% if @post213.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post213.errors.count, "error") %> prohibited this post213 from being saved:</h2>

      <ul>
      <% @post213.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
