<%= form_for(@post38) do |f| %>
  <% if @post38.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post38.errors.count, "error") %> prohibited this post38 from being saved:</h2>

      <ul>
      <% @post38.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
