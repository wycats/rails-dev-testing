<%= form_for(@post142) do |f| %>
  <% if @post142.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post142.errors.count, "error") %> prohibited this post142 from being saved:</h2>

      <ul>
      <% @post142.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
