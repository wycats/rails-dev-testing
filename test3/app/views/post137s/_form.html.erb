<%= form_for(@post137) do |f| %>
  <% if @post137.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post137.errors.count, "error") %> prohibited this post137 from being saved:</h2>

      <ul>
      <% @post137.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
