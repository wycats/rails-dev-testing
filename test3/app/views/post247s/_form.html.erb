<%= form_for(@post247) do |f| %>
  <% if @post247.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post247.errors.count, "error") %> prohibited this post247 from being saved:</h2>

      <ul>
      <% @post247.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
