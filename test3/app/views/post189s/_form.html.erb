<%= form_for(@post189) do |f| %>
  <% if @post189.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post189.errors.count, "error") %> prohibited this post189 from being saved:</h2>

      <ul>
      <% @post189.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
