<%= form_for(@post239) do |f| %>
  <% if @post239.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post239.errors.count, "error") %> prohibited this post239 from being saved:</h2>

      <ul>
      <% @post239.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
