<%= form_for(@post39) do |f| %>
  <% if @post39.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post39.errors.count, "error") %> prohibited this post39 from being saved:</h2>

      <ul>
      <% @post39.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
