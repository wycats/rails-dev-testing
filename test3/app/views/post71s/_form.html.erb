<%= form_for(@post71) do |f| %>
  <% if @post71.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post71.errors.count, "error") %> prohibited this post71 from being saved:</h2>

      <ul>
      <% @post71.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
