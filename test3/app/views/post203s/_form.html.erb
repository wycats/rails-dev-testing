<%= form_for(@post203) do |f| %>
  <% if @post203.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post203.errors.count, "error") %> prohibited this post203 from being saved:</h2>

      <ul>
      <% @post203.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
