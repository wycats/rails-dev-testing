<%= form_for(@post143) do |f| %>
  <% if @post143.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post143.errors.count, "error") %> prohibited this post143 from being saved:</h2>

      <ul>
      <% @post143.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
