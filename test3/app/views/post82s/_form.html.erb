<%= form_for(@post82) do |f| %>
  <% if @post82.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post82.errors.count, "error") %> prohibited this post82 from being saved:</h2>

      <ul>
      <% @post82.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
