<%= form_for(@post96) do |f| %>
  <% if @post96.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post96.errors.count, "error") %> prohibited this post96 from being saved:</h2>

      <ul>
      <% @post96.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
