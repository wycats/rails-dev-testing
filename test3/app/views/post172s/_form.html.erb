<%= form_for(@post172) do |f| %>
  <% if @post172.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post172.errors.count, "error") %> prohibited this post172 from being saved:</h2>

      <ul>
      <% @post172.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
