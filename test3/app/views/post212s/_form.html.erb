<%= form_for(@post212) do |f| %>
  <% if @post212.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post212.errors.count, "error") %> prohibited this post212 from being saved:</h2>

      <ul>
      <% @post212.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
