<%= form_for(@post192) do |f| %>
  <% if @post192.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post192.errors.count, "error") %> prohibited this post192 from being saved:</h2>

      <ul>
      <% @post192.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
