<%= form_for(@post151) do |f| %>
  <% if @post151.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post151.errors.count, "error") %> prohibited this post151 from being saved:</h2>

      <ul>
      <% @post151.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
