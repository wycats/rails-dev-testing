<%= form_for(@post183) do |f| %>
  <% if @post183.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post183.errors.count, "error") %> prohibited this post183 from being saved:</h2>

      <ul>
      <% @post183.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
