<%= form_for(@post123) do |f| %>
  <% if @post123.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post123.errors.count, "error") %> prohibited this post123 from being saved:</h2>

      <ul>
      <% @post123.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
