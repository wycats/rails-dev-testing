<%= form_for(@post177) do |f| %>
  <% if @post177.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post177.errors.count, "error") %> prohibited this post177 from being saved:</h2>

      <ul>
      <% @post177.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
