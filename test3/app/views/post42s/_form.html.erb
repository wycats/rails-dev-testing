<%= form_for(@post42) do |f| %>
  <% if @post42.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post42.errors.count, "error") %> prohibited this post42 from being saved:</h2>

      <ul>
      <% @post42.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
