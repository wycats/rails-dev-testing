<%= form_for(@post128) do |f| %>
  <% if @post128.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post128.errors.count, "error") %> prohibited this post128 from being saved:</h2>

      <ul>
      <% @post128.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
