<%= form_for(@post208) do |f| %>
  <% if @post208.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post208.errors.count, "error") %> prohibited this post208 from being saved:</h2>

      <ul>
      <% @post208.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
