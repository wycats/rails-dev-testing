<%= form_for(@post190) do |f| %>
  <% if @post190.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post190.errors.count, "error") %> prohibited this post190 from being saved:</h2>

      <ul>
      <% @post190.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
