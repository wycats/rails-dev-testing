<%= form_for(@post246) do |f| %>
  <% if @post246.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post246.errors.count, "error") %> prohibited this post246 from being saved:</h2>

      <ul>
      <% @post246.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
