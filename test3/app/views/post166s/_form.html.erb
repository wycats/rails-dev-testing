<%= form_for(@post166) do |f| %>
  <% if @post166.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post166.errors.count, "error") %> prohibited this post166 from being saved:</h2>

      <ul>
      <% @post166.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
