<%= form_for(@post99) do |f| %>
  <% if @post99.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post99.errors.count, "error") %> prohibited this post99 from being saved:</h2>

      <ul>
      <% @post99.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
