<%= form_for(@post210) do |f| %>
  <% if @post210.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post210.errors.count, "error") %> prohibited this post210 from being saved:</h2>

      <ul>
      <% @post210.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
