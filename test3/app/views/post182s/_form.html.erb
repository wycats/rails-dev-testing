<%= form_for(@post182) do |f| %>
  <% if @post182.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post182.errors.count, "error") %> prohibited this post182 from being saved:</h2>

      <ul>
      <% @post182.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
