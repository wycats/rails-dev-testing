<%= form_for(@post26) do |f| %>
  <% if @post26.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post26.errors.count, "error") %> prohibited this post26 from being saved:</h2>

      <ul>
      <% @post26.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
