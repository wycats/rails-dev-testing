<%= form_for(@post131) do |f| %>
  <% if @post131.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post131.errors.count, "error") %> prohibited this post131 from being saved:</h2>

      <ul>
      <% @post131.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
