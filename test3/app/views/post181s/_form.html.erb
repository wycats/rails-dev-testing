<%= form_for(@post181) do |f| %>
  <% if @post181.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post181.errors.count, "error") %> prohibited this post181 from being saved:</h2>

      <ul>
      <% @post181.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
