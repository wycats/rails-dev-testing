<%= form_for(@post108) do |f| %>
  <% if @post108.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post108.errors.count, "error") %> prohibited this post108 from being saved:</h2>

      <ul>
      <% @post108.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
