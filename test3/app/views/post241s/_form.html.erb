<%= form_for(@post241) do |f| %>
  <% if @post241.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post241.errors.count, "error") %> prohibited this post241 from being saved:</h2>

      <ul>
      <% @post241.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
