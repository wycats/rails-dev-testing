<%= form_for(@post175) do |f| %>
  <% if @post175.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post175.errors.count, "error") %> prohibited this post175 from being saved:</h2>

      <ul>
      <% @post175.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
