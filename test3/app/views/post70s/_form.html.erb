<%= form_for(@post70) do |f| %>
  <% if @post70.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post70.errors.count, "error") %> prohibited this post70 from being saved:</h2>

      <ul>
      <% @post70.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
