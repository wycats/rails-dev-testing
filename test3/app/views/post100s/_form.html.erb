<%= form_for(@post100) do |f| %>
  <% if @post100.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post100.errors.count, "error") %> prohibited this post100 from being saved:</h2>

      <ul>
      <% @post100.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
