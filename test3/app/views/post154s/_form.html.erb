<%= form_for(@post154) do |f| %>
  <% if @post154.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post154.errors.count, "error") %> prohibited this post154 from being saved:</h2>

      <ul>
      <% @post154.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
