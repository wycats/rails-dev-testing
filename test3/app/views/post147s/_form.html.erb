<%= form_for(@post147) do |f| %>
  <% if @post147.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post147.errors.count, "error") %> prohibited this post147 from being saved:</h2>

      <ul>
      <% @post147.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
