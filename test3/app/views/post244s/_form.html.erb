<%= form_for(@post244) do |f| %>
  <% if @post244.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post244.errors.count, "error") %> prohibited this post244 from being saved:</h2>

      <ul>
      <% @post244.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
