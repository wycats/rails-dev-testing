<%= form_for(@post193) do |f| %>
  <% if @post193.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post193.errors.count, "error") %> prohibited this post193 from being saved:</h2>

      <ul>
      <% @post193.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
