<%= form_for(@post122) do |f| %>
  <% if @post122.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post122.errors.count, "error") %> prohibited this post122 from being saved:</h2>

      <ul>
      <% @post122.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
