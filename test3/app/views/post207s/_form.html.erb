<%= form_for(@post207) do |f| %>
  <% if @post207.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post207.errors.count, "error") %> prohibited this post207 from being saved:</h2>

      <ul>
      <% @post207.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
